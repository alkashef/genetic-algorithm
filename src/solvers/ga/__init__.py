"""
src/solvers/ga/__init__.py

Self-contained genetic-algorithm solver: parameters (ga_params.py), the
public per-stage service API (ga_service.py), and the operator engine
(ga_pipeline.py). Contains no logic itself.
"""
